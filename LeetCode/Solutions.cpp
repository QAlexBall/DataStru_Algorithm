//
// Created by alex on 2/14/19.
//
#include <iostream>
#include <vector>
using namespace std;

struct ListNode {
    int val;
    ListNode *next;
    ListNode(int x) : val(x), next(NULL) {}
};

class Soution {
public:
    ListNode* merget_k_lists(vector<ListNode*>& lists) {

    }
};